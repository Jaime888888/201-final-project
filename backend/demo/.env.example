# Local demo values. Replace the passwords and signing key before shared use.
MYSQL_ROOT_PASSWORD=replace-with-a-local-root-password
MYSQL_DATABASE=appdb
MYSQL_USER=appuser
MYSQL_PASSWORD=replace-with-a-local-app-password
SPRING_DATASOURCE_URL=jdbc:mysql://127.0.0.1:33061/appdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
JWT_SECRET=replace-with-at-least-32-random-characters
CORS_ALLOWED_ORIGINS=http://localhost:3000
